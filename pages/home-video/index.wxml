<view class="video">
	<!-- block中自定义组件flex样式存在问题，所以使用view进行遍历循环 -->
	<view class="item" wx:for="{{topMVs}}" wx:key="id">
		<video-item-v1 item="{{item}}" bindtap="handleVideoItemClick" data-aaa="{{item}}"></video-item-v1>
	</view>
</view>